# FieldRelay Application

Reserved for the Ionic Angular responsive PWA and Capacitor mobile application.

Implementation must follow the authoritative documents under `docs/` and the visual references under `mockups/`.
